
@defstx @onload() {
	setloadpath(push(loadpath(),"."));
	return #[];
}
@onload();

@add_loadpath(loadpath()[0] + "/../test/libs");

evalk("@export(lalala);",
    @lambda(r) { r; },
    @lambda(msg) {});

evalk("@import(lalala);",
    @lambda(r) { r; },
    @lambda(msg) {});

evalk("@with_imports(recursive) {};",
    @lambda(r) { r; },
    @lambda(msg) {});

