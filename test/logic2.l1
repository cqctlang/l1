0 || print(1);
1 || print(0);

x = 1 || 1;
print(x);

x = 1 || 0;
print(x);

x = 0 || 1;
print(x);

x = 0 || 0;
print(!x);

if(1 || 1)
	print(1);
else
	print(0);

if(1 || 1)
	print(1);

if(1 || 0)
	print(1);
else
	print(0);

if(1 || 0)
	print(1);

if(0 || 1)
	print(1);
else
	print(0);

if(0 || 1)
	print(1);

if(0 || 0)
	print(0);
else
	print(1);

if(0 || 0)
	print(0);

if(0 || 0 || 1)
	print(1);
else
	print(0);

if(0 || 1 || 0)
	print(1);
else
	print(0);

if(1 || 0 || 0)
	print(1);
else
	print(0);

if(0 || 0 || 0)
	print(0);
else
	print(1);
