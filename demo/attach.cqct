@include <ctlmux.cqct>

define test1(pid)
{
	@local mux, as;
	mux = mkctlmux();
	as = mux.attach(pid);
	as.trace(ctlmux`Fsyscall);
	mux.run();
	return nil;
}

if(length(args) > 1)
	test1(strton(args[1]));
else
	printf("usage: %s <pid>\n", args[0]);

		
