@include <pras.l1>

@global as;
define init()
{
	as = launchlocalprocredir("/home/vczandy/bin/prctl", "-c");
	as.xcont();
}

define test()
{
	as.xfwrite(0, "hello!");
	s = as.xfread(1, 100);
	printf("got %a\n", s);
}
