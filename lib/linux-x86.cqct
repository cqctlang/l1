linuxns = @names c32le {
	typedef
	enum Syscall {
	} Syscall;

	typedef
	struct Ctx {
		@0;
	} Ctx;
};

unix = @names c32le {

	@include <linux-x86-enum.names>
	@include <linux-unix.names>

	struct stat {
		@0x10 int32 st_mode;
		@0x2c uint64 st_size;
		@0x60;
	};
};

