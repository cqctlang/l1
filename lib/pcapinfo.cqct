#!/usr/bin/env l1
@include <pcap.cqct>

@define main(args)
{
	if (length(args) != 1) {
		printf("usage: pcapinfo <pcapfile>\n");
		return 0;
	}

	pdom = mkpcapdom(listref(args, 0));
	pcap_info(pdom);
	pcap_dump(pdom);

	return 0;
}


return main(args);
