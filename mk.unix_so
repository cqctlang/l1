UNIX = fnfs fnio fnnet fnsys fnsec fnzlib 
L1LIBS += -lz -lcrypto

L1EXTRAS := $(addsuffix .c, $(UNIX)) \
	     unix.c

L1FUNS   := $(UNIX)

TARG      := libl1.so
CFLAGS    += -fPIC -nostdlib

libl1.so: l1.o
	$(CC) -shared -Xlinker -Bsymbolic -o $@ $^
